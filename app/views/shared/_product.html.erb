<%
start ||= 0
counter = product_counter + start
classes ||= ""
preid ||= ""
thumb ||= false
if thumb
    image = product.image_url(:thumb)
else
    image = product.image_url
end
%>
<%= link_to image_tag(image.to_s, {:id => preid + counter.to_s, :class => classes}), product %>
